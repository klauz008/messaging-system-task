function sendMessage() {
    let message = document.getElementById("message-box");
    let html = "<div class="Message-boxmy-message-box">" + "<div class="message my-message"> "  + message + " </div>" + "<div class="seperator"></div>" + "</div>";

}